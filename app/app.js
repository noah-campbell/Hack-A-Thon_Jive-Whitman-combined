(function () {
	'use strict'

	angular.module('app', []);
	// creating a shared service to connect data across multiple controllers
	angular.module('app').service('SharedService', function () {
		return (response);
	});
	
})();